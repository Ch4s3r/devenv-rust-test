name: "Build"

on:
  pull_request:
  push:

jobs:
  tests:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    defaults:
      run:
        shell: bash -c "nix develop --impure -c bash -- {0}"

    steps:
    - uses: actions/checkout@v7
    - uses: DeterminateSystems/determinate-nix-action@main
    - uses: DeterminateSystems/magic-nix-cache-action@main
      with:
        use-flakehub: false
    - uses: DeterminateSystems/flake-checker-action@main

    - name: Run tests
      run: devenv test

    - name: Build release artifact
      run: nix build .#default

    - name: Code-sign macOS release artifact
      if: matrix.os == 'macos-latest'
      env:
        MACOS_CODESIGN_P12_PASSWORD: ${{ secrets.MACOS_CODESIGN_P12_PASSWORD }}
        MACOS_CODESIGN_P12_BASE64: ${{ secrets.MACOS_CODESIGN_P12_BASE64 }}
      run: nix run .#sign-aarch64-darwin

    - name: Upload release artifact
      uses: actions/upload-artifact@v4
      with:
        name: devenv-rust-test-${{ matrix.os }}
        path: ${{ matrix.os == 'macos-latest' && './devenv-rust-test' || 'result/bin/devenv-rust-test' }}
